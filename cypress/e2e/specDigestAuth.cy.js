describe('Digest Authentication', () => {
  it.skip('No soportado por Cypress (UI ni API)', () => {
    // Cypress no implementa el handshake completo de HTTP Digest Authentication.
    // El endpoint /digest_auth no puede ser automatizado ni a nivel UI ni API.
  })
})
